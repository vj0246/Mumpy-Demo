# Frontend -> backend URL.
# Local dev can leave this unset (defaults to http://localhost:8000).
# On Vercel, set VITE_API_BASE to your Render backend URL (no trailing slash):
VITE_API_BASE=https://mumpy-demo-api.onrender.com
